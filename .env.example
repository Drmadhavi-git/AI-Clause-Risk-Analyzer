# Optional: If you want LLM-based explanations (better output).
OPENAI_API_KEY=

# Optional: use a custom OpenAI base URL (leave blank usually)
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: model name (works if your OpenAI account supports it)
OPENAI_MODEL=gpt-4o-mini
